import { categoryMock } from '../../category/__mocks__/category.mock';
import { AddProductDto } from '../dtos/add-product.dto';

export const addProductMock: AddProductDto = {
  amount: 1,
  productId: 1,
};